# if any(service for service in  if "search for css in youtube".find(service)!=-1):
#     print("True")

# if any(service for service in ["youtube" , "google"] if search_term.count(service)):
#     pass


# even_numbers = lambda range_ : [num for num in range(range_) if num % 2 ==0 ]
# print(even_numbers(20)[::-1])

# num = 1
# num *=10
#add this Qalm
# test = {"name" : 'jane', 'age':20}
# test.update({"age":21})
# print([[key , value] for key ,value in test.items()])
